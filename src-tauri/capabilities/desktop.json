{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "desktop",
  "description": "Local document windows can listen for backend close requests.",
  "windows": ["doc-*"],
  "permissions": ["core:event:allow-listen", "core:event:allow-unlisten", "core:window:allow-set-title", "updater:default"]
}
