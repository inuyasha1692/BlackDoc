# Windows 客户端

## 架构

React + BlockNote 负责编辑；`src/desktop.ts` 定义桌面命令接口；`src-tauri` 负责本地文件、草稿、窗口、文件关联及安装包。

每窗口拥有自己的编辑器和保存状态。后端登记窗口与源文件的关联，规范化路径并阻止同一文件在多个窗口中同时写入。新文档扩展名为 `.bdoc`，旧 `.blackdoc` 文件继续支持打开和原路径保存。内容保持 BlockNote 原生 JSON 数组。

正式版本加载打包后的前端资源，离线编辑无需启动开发服务器。远程图片等外部资源仍取决于网络或是否已内嵌。

## 开发与发布

- 日常：双击仓库根目录中的 `启动 BlackDoc 开发版.cmd`，或运行 `npm run desktop:dev`。
- 前端检查：`npm run lint`、`npm test`、`npm run build`。
- Rust 检查：`cargo test --manifest-path src-tauri/Cargo.toml`。
- 安装包：在 PowerShell 中设置 `$env:TAURI_SIGNING_PRIVATE_KEY = Join-Path $env:USERPROFILE '.tauri\blackdoc.key'` 和 `$env:TAURI_SIGNING_PRIVATE_KEY_PASSWORD = ''`，然后运行 `npm run desktop:build`。
- 构建后运行 `npm run release:metadata`，将 NSIS 安装包和同目录生成的 `latest.json` 上传到版本号对应的 GitHub Release。`latest.json` 内含安装包签名；发布前须确认 URL 指向同一文件。
- 只有验证安装、卸载、文件关联以及正式发行时需要安装新包。
- 私钥只保存在当前用户目录中，不能上传仓库或发布页。请妥善备份；丢失后已安装客户端无法验证以后发布的更新。
- 客户端发现新版本后可下载更新包并查看进度；下载完成后先关闭其他文档窗口，再点击“重启并安装”。当前窗口会先保存内容，Windows 安装器会在退出客户端后启动并完成更新。

## 验收步骤

1. 安装后从桌面或开始菜单启动，确认编辑区域、格式工具栏、大纲和块链接可用。
2. 启动后在空白未命名窗口点击“打开”，选择 `files/BlackDoc功能展示示例.bdoc`，确认载入当前窗口；另存为临时目录中的 `.bdoc`，确认可重新打开且内容完整。
3. 新建两个窗口，分别输入不同内容，确认自动保存状态独立；分别保存到不同路径。
4. 双击不同 `.bdoc` 文件，确认各有一个窗口；再次双击同一文件，确认激活已有窗口。
5. 从窗口 A 另存为窗口 B 已打开的路径，确认拒绝覆盖，B 的文件保持完整。
6. 编辑后立即关闭窗口，选择取消、保存并继续、放弃更改，分别确认行为；取消保存路径选择后窗口仍存在。
7. 在多个新文档中输入内容并等待草稿暂存，模拟进程异常退出，再启动，确认每份草稿分别恢复。
8. 导出单文件 HTML，取消选择时不提示成功；成功导出后独立浏览器打开，检查图片和内部链接。
9. 检查中文路径、空格路径、旧 `.blackdoc` 文件可打开，以及其他扩展名被拒绝。
10. 编辑器打开源文件后用其他程序修改该文件，再保存，确认冲突提示，避免无提示覆盖。
11. 空白窗口取消打开或选择损坏文件，确认仍可编辑；未命名文档输入内容并等待草稿暂存后打开文件，确认另开窗口并保留原文档；空白窗口打开已在其他窗口打开的文件，确认聚焦已有窗口。
12. 使用 /两列 和 /三列 插入官方多栏，拖动列宽并在列间移动普通块；保存、重新打开和导出 HTML 后确认列数、顺序、宽度及内容保留。

测试使用源文件副本；示例源文档中的用户修改应保留。

## 本次验证（2026-09-22）

- 前端 26 项测试、Rust 12 项测试、ESLint 和前端生产构建通过。
- Windows x64 NSIS 安装包生成于 `src-tauri/target/release/bundle/nsis/`。
- 实际运行正式版客户端，确认编辑器、大纲及功能展示文档正常显示，控制台无错误。
- 使用中文路径临时副本验证文件打开、独立窗口、重复打开复用窗口；修改检查项后确认自动保存确实写回副本。
- 补充启动恢复尚未完成时关闭窗口的回归测试，防止删除待恢复草稿。
- 尚未完成安装/卸载、安装后双击文件关联、原生文件选择器完整流程及异常退出恢复的端到端验收。相关单元测试不能替代这些验收。
